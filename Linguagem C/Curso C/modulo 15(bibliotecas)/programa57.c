#include <stdio.h>
#include <math.h>

int main(){

	double a = 0;
	double b = 1;
	double c = 2;

	printf("O exponencial de %lf é %lf\n", a, exp(a));
	printf("O exponencial de %lf é %lf\n", b, exp(b));
	printf("O exponencial de %lf é %lf\n", c, exp(c));

	return 0;
}
